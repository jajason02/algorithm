### 💡 로직의 원리와 힌트

**1. 재귀는 '나 자신을 호출하는 것'이 아니라, '일을 쪼개서 아래로 넘기는 것'입니다.**
함수가 호출될 때마다 메모리의 **'콜 스택(Call Stack)'** 영역에는 해당 함수의 매개변수, 지역 변수, 그리고 돌아갈 주소값이 쌓입니다. 이를 **스택 프레임**이라고 합니다.

**2. 반드시 끝이 있어야 합니다 (Base Case).**
스택은 무한하지 않습니다. 종료 조건이 없으면 `RecursionError` (Stack Overflow)가 발생합니다. 마치 끝이 막힌 원통에 계속 물건을 쌓다가 넘치는 것과 같습니다.

**3. "LIFO(Last In, First Out)"의 마법을 기억하세요.**
가장 나중에 호출된 함수가 가장 먼저 완료되어 스택에서 빠져나갑니다. 이때, 밑에서 기다리던 함수들은 위에서 가져온 결과값을 받아 자기 할 일을 마무리합니다.

---


# 📚 알고리즘 노트: Stack Recursion (재귀)

## [개요]

**재귀(Recursion)** 란 함수 내부에서 자기 자신을 다시 호출하여, 큰 문제를 동일한 구조의 작은 문제로 나누어 해결하는 프로그래밍 기법입니다.

---

## [로직 추적]

예시: `factorial(3)`을 호출했을 때의 메모리(Call Stack) 변화

| 단계 | 실행 상태 | 스택 프레임 (Stack Frame) | 비고 |
| --- | --- | --- | --- |
| **1** | `fact(3)` 호출 | `[fact(3)]` | `3 * fact(2)` 대기 |
| **2** | `fact(2)` 호출 | `[fact(3), fact(2)]` | `2 * fact(1)` 대기 |
| **3** | `fact(1)` 호출 | `[fact(3), fact(2), fact(1)]` | **Base Case 도달 (return 1)** |
| **4** | `fact(1)` 반환 | `[fact(3), fact(2)]` | `fact(2)`는 `2 * 1`을 수행 |
| **5** | `fact(2)` 반환 | `[fact(3)]` | `fact(3)`은 `3 * 2`를 수행 |
| **6** | `fact(3)` 종료 | `[]` | 최종값 **6** 반환 |

---

## [코드]

파이썬의 가독성과 재귀 깊이 제한을 고려한 최적의 코드입니다.

```python
import sys

# 실무 팁: 파이썬은 재귀 깊이 제한이 기본 1000회입니다. 
# 필요 시 아래 코드로 늘릴 수 있습니다.
sys.setrecursionlimit(2000)

def factorial(n: int) -> int:
    """
    재귀를 이용한 팩토리얼 계산
    :param n: 계산할 정수
    :return: n! 결과값
    """
    # [Step 1: Base Case] 종료 조건 (없으면 무한 루프)
    if n <= 1:
        print(f"--- Base Case 도달: n={n}, 스택 반환 시작 ---")
        return 1
    
    # [Step 2: Recursive Step] 
    # 현재 단계의 로직을 수행하고 다음 재귀로 넘김
    print(f"함수 호출: n={n} (스택에 push)")
    result = n * factorial(n - 1)
    
    # [Step 3: Returning]
    # 스택에서 빠져나오며 최종 계산 수행
    print(f"함수 종료: {n} * factorial({n-1}) = {result} (스택에서 pop)")
    return result

if __name__ == "__main__":
    print(f"최종 결과: {factorial(3)}")

```

### 🔍 디버깅 모드 가이드

1. **Breakpoint 설정**: `if n <= 1:` 라인과 `result = n * factorial(n - 1)` 라인에 브레이크포인트를 거세요.
2. **Variables 창 확인**: 호출될 때마다 `n`의 값이 줄어들며 새로운 스택 프레임이 생성되는 것을 확인하세요.
3. **Call Stack 창 확인**: 함수가 종료되지 않고 계속 쌓이다가, `n=1`일 때 위에서부터 하나씩 제거(Pop)되는 순서를 유심히 관찰하십시오.

---

## [분석]

* **장점**: 코드가 간결하고 알고리즘의 수식(점화식)을 그대로 코드로 옮길 수 있어 가독성이 좋습니다. 특히 트리나 그래프 탐색(DFS)에 최적화되어 있습니다.
* **단점**: 함수 호출마다 스택 메모리를 사용하므로 **메모리 오버헤드**가 발생합니다. `n`이 너무 크면 **Stack Overflow**가 발생할 수 있습니다.
* **실무 꿀팁**: 재귀가 너무 깊어진다면 **꼬리 재귀 최적화(Tail Recursion Optimization)** 를 고려하거나, 반복문(Iteration)과 명시적인 **Stack 자료구조**를 사용하는 방식으로 변환하는 것이 안전합니다.

---

## [활용]

재귀 개념을 마스터하면 다음 문제들을 정복할 수 있습니다.

* **탐색**: DFS(깊이 우선 탐색), 이진 탐색(Binary Search)
* **정렬**: 퀵 정렬(Quick Sort), 병합 정렬(Merge Sort)
* **수학**: 피보나치 수열, 하노이의 탑, 유클리드 호제법(최대공약수)
* **기타**: 조합/순열 생성(Backtracking)

---
